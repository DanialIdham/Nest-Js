import { CompanyProfileEntity } from './company-profile.entity';

describe('CompanyProfileEntity', () => {
  it('should be defined', () => {
    expect(new CompanyProfileEntity()).toBeDefined();
  });
});
